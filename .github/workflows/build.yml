name: 自动构建与发布

on:
  push:
    branches:
      - main
      - dev
      - hotfix/*

jobs:
  build:
    name: PyInstaller构建
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
      - name: Checkout 代码
        uses: actions/checkout@v3
      
      - name: 安装Python
        uses: actions/setup-python@v3
        with:
          python-version:  "3.11.7"
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: 安装PyInstaller
        run: pip install pyinstaller
      
      - name: 通过PyInstaller构建
        run: pyinstaller GUI.spec
      
      - name: Upload (Windows)
        if: ${{ runner.os == 'windows' }}
        uses: actions/upload-artifact@v3
        with:
          name: CCZU-iCal-win_x86
          path: dist/*.exe

      - name: Upload (macOS)
        if: ${{ runner.os == 'macos' }}
        uses: actions/upload-artifact@v3
        with:
          name: CCZU-iCal-macOS_x86
          path: dist/*.app

      - name: Upload (Linux)
        if: ${{ runner.os == 'linux' }}
        uses: actions/upload-artifact@v3
        with:
          name: CCZU-iCal-linux
          path: dist/*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download
        uses: actions/download-artifact@v3

      - name: Display files
        run: ls -R

      - name: Move files
        run: |
          cp -r CCZU-iCal_mac_x86/CCZU-iCal.app .
          cp CCZU-iCal_windows/CCZU-iCal.exe .
          cp CCZU-iCal_linux/CCZU-iCal .

      - name: ZIP
        run: |
          zip -r CCZU-iCal_mac_x86.zip CCZU-iCal.app
          zip CCZU-iCal_windows.zip CCZU-iCal.exe
          zip CCZU-iCal_linux.zip CCZU-iCal

      - name: Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: tag
          draft: true
          files: |
            CCZU-iCal_windows.zip
            CCZU-iCal_mac_x86.zip
            CCZU-iCal_linux.zip
          name: 🎉
          body: |
            ## 新增
            - 新增内容
            
            ## 优化
            - 优化内容
